---
alwaysApply: true
globs:
  - apps/api/**
---

# Backend Advisor — IPCAS Monorepo

Django 4.2 + Django REST Framework API in `apps/api/`. PostgreSQL is required (`DATABASE_URL`). No SQLite fallback.

## Stack

- **Framework:** Django + DRF
- **Database:** PostgreSQL (via `dj-database-url` + `psycopg`)
- **Auth:** JWT (`djangorestframework-simplejwt`), Django `Group` for roles
- **API docs:** drf-spectacular at `/api/schema/`, `/api/docs/`
- **Excel:** pandas + openpyxl in services/views

## Architecture

- Monolithic API under `apps/api/core/`
- Apps: `authentication`, `business_meta`, `inventory`, `config`
- Business logic in `services.py`; thin views/viewsets
- Dynamic row data in `DynamicTableRow` (PostgreSQL `JSONField`)

## Code organization

- Route handlers delegate to services (DIP)
- Serializers: validation/serialization only
- Models: data structure only
- Use pytest or Django `TestCase` for tests

## API documentation

Document endpoints with drf-spectacular: `summary`, `description`, `tags`, response schemas.

## Security

- JWT on protected endpoints
- Permission classes in `authentication/permissions.py` and `business_meta/permissions.py`
- Validate inputs; use ORM/queryset filtering (no raw SQL from user strings)

## Blueprint reference

Full target spec: `docs/IPCAS_Engineering_Blueprint.md`. Current code maps `Business` → blueprint `Project`. See `docs/ipcas-scope-map.md`.
