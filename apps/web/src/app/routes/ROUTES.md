# Routes Routes

This directory contains the frontend routing configurations for `routes`.

## Purpose

The routes defined here map URL paths to React components and handle the layout structure of the application.

## Routes

- Refer to the `.ts` files for specific path and route definitions.
