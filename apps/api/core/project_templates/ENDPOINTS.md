# Project_templates Endpoints

This directory contains the URL routing configurations for the `project_templates` app.

## Purpose

The endpoints defined here handle requests related to `project_templates` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
