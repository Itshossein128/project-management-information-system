# Subcontractors Endpoints

This directory contains the URL routing configurations for the `subcontractors` app.

## Purpose

The endpoints defined here handle requests related to `subcontractors` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
