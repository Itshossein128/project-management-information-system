# Schedule Endpoints

This directory contains the URL routing configurations for the `schedule` app.

## Purpose

The endpoints defined here handle requests related to `schedule` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
