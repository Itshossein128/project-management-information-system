# Wbs Endpoints

This directory contains the URL routing configurations for the `wbs` app.

## Purpose

The endpoints defined here handle requests related to `wbs` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
