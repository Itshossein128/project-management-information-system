# Hr Endpoints

This directory contains the URL routing configurations for the `hr` app.

## Purpose

The endpoints defined here handle requests related to `hr` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
