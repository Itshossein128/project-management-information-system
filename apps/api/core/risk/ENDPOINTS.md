# Risk Endpoints

This directory contains the URL routing configurations for the `risk` app.

## Purpose

The endpoints defined here handle requests related to `risk` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
