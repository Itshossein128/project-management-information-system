# Cash_flow Endpoints

This directory contains the URL routing configurations for the `cash_flow` app.

## Purpose

The endpoints defined here handle requests related to `cash_flow` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
