# Storage Endpoints

This directory contains the URL routing configurations for the `storage` app.

## Purpose

The endpoints defined here handle requests related to `storage` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
