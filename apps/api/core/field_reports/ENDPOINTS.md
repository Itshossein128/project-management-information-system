# Field_reports Endpoints

This directory contains the URL routing configurations for the `field_reports` app.

## Purpose

The endpoints defined here handle requests related to `field_reports` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
