# Projects Endpoints

This directory contains the URL routing configurations for the `projects` app.

## Purpose

The endpoints defined here handle requests related to `projects` and map them to their respective views/viewsets.

## Endpoints

- See the respective `urls.py` files for detailed path configurations.
